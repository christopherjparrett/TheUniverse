# =============================================================================
# PLANETARY INFO PROJECT - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and update the values for your environment
# Never commit the actual .env file to version control

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database settings for the backend service
POSTGRES_DB=planets_db
POSTGRES_USER=planets_user
POSTGRES_PASSWORD=your_secure_database_password_here

# =============================================================================
# JWT AUTHENTICATION CONFIGURATION
# =============================================================================
# Secret key for JWT token signing (must be at least 32 characters)
# Generate a strong random key for production: openssl rand -hex 32
JWT_SECRET_KEY=your_super_secret_jwt_key_change_in_production_make_it_long_and_random_at_least_32_characters

# JWT algorithm (default: HS256)
JWT_ALGORITHM=HS256

# Token expiration time in minutes (default: 30)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# BACKEND SERVICE CONFIGURATION
# =============================================================================
# Database connection URL (automatically constructed from POSTGRES_* variables)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://planets_user:your_secure_database_password_here@db:5432/planets_db

# =============================================================================
# FRONTEND SERVICE CONFIGURATION
# =============================================================================
# API base URL for frontend to communicate with backend
# Default: http://localhost:8000 (for local development)
REACT_APP_API_URL=http://localhost:8000

# Node environment (production/development)
NODE_ENV=production

# =============================================================================
# DOCKER COMPOSE SERVICE CONFIGURATION
# =============================================================================
# Optional: Mock server port (default: 4010)
# Enable with: docker-compose --profile mock up -d
MOCK_SERVER_PORT=4010

# =============================================================================
# DEVELOPMENT OVERRIDES (Optional)
# =============================================================================
# Uncomment and modify these for local development overrides

# For local development with SQLite instead of PostgreSQL:
# DATABASE_URL=sqlite:///./planets.db

# For different backend port:
# REACT_APP_API_URL=http://localhost:8001

# For different frontend port in docker-compose:
# Modify ports section in docker-compose.yml: "3001:80"
